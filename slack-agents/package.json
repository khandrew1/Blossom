{
  "name": "blossom-slack-agents",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Phone-friendly controller for staged Blossom Hill Cafe Slack demo messages.",
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "test": "npm run build && node --test dist/test/**/*.test.js",
    "check": "npm test"
  },
  "dependencies": {
    "@slack/web-api": "^7.10.0",
    "dotenv": "^17.2.0",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.3",
    "@types/node": "^24.1.0",
    "tsx": "^4.20.3",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
